{
  "name": "kirana-inventory",
  "version": "1.0.0",
  "private": true,
  "description": "Kirana Shop Inventory Management System - offline-first PWA with Express/PostgreSQL backend",
  "workspaces": [
    "server",
    "web",
    "scripts"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "build": "npm -w server run build && npm -w web run build",
    "typecheck": "npm -w server run typecheck && npm -w web run typecheck",
    "start:server": "npm -w server run start",
    "start:web": "npm -w web run preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "license": "MIT"
}
